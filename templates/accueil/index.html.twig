{% extends 'base.html.twig' %}

{% block title %}Hello AccueilController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">

        {%if app.user %}

        <h1>Bienvenue {{ app.user.prenom }} !</h1>


 {% if is_granted('ROLE_ELEVE') %}

                    <div class="card">
            <div class="card-body">
                <h3 class="card-title">Mes leçons</h3>

                {% for  lecon in app.user.inscriptionsLecons %}
                            
                  <p>  <a href='{{ path('app_lecon_show', {'id': lecon.id}) }}'> {{lecon.nom}} de {{lecon.createur.prenom}} {{lecon.createur.nom}} </a></p>
                            
                    {% else %}
                        
                            <p>Aucune inscription !</p>
                        
                    {% endfor %}

            </div>
            </div>
        {% endif %}

        {% else %}

            <h1>Bienvenue utilisateur anonyme...</h1>

        {% endif %}
</div>





{% endblock %}
