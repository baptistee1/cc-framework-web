<form method="post" action="{{ path('app_lecon_delete', {'id': lecon.id}) }}" onsubmit="return confirm('Voulez vous vraiment supprimer la leçon ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ lecon.id) }}">
    <button class="btn btn-danger">Supprimer la leçon</button>
</form>
